# 服务器运维插件（server_ops）下一步优化方案 · 294 之后

线上基线：**1.20.37 (294)**，四条安全线（C1 通道凭据 / C2 只读+写档 / C3 资源告警 / C4 暴露面收口）
全部落地。本方案承接 `docs/server_ops_plugin_next_steps_291.md`（A1–A13 / B2 / C1–C4），
**只写它列而未做的、以及 291 之后新出现或新探明的**；不重复已否决项（见第五节）。

行号均按 **294 的代码**回读核对；活体数字为 **2026-09-26** 实测。

## 一、事实基线（先看偏差）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 插件又长了：`server_ops/` **8288 行**（291 时 5976），测试 **6617 行**（291 时 4942） | `wc -l` 逐文件 |
| 2 | A11 仍未做：条目上限/继续加载/取消**零命中** | `grep -rn "继续加载\|maxEntries\|visibleCount" lib/.../server_ops/` 空 |
| 3 | A12 仍未做：`Range` **零命中**（唯一命中是 `removeRange`，误报） | `grep -rn "Range\|resume" lib/.../server_ops/` |
| 4 | **大目录的代价比 291 实测翻了一倍**：`/usr/lib64`（758 条目）→ **484 KB / 53.97 秒**；291 当时是 485 KB / 30.5 秒 | 见 1.2 复现；`/etc` 129 KB / 1.83 s、`/root` 148 KB / 2.70 s（同一次实测） |
| 5 | **上传侧带 `Content-Range` 会被静默截断**（291 没写这条）：`PUT Content-Range: bytes 5-9/10` + 5 字节体 → `201`，文件**变成 5 字节** | 见 1.3 实测 |
| 6 | 下载侧 Range 完全正确：`bytes=5-` / `0-4` / `-3` 三式全 `206`，内容对得上 | 见 1.3 实测 |
| 7 | **`logs`（服务端日志尾部）是有接口、有测试、没界面的半成品**：服务端 `act_logs` 可用（真跑 `/www/wwwlogs/access.log` 返回 2 行），客户端方法存在，**`lib/` 内零引用** | `box_ops_api.py:466`（`act_logs`）、`server_ops_api_client.dart:229`、`lib/` 内 `logs(` 零命中 |
| 8 | **现行闸门看不见第 7 条这种现象**：`scan_dangling_apis.py` 把 `test/` 里的调用也算调用点（`SEARCH = ["lib", "test"]`），所以"只有测试在调"不会浮出来 | `tool/scan_dangling_apis.py:48`（`SEARCH`）、`:82-93`（计数逻辑）；该脚本现在报"悬挂 0"仍然成立，但**口径比它读起来弱** |
| 9 | 服务抽屉里的"最近日志"是**另一个接口**（`service(unit, lines)` = journal），已接线；`logs(path)` 管的是**日志文件**，两者不重复 | `system_tab.dart:655`（抽屉注释与 `service()` 调用）、`server_ops_api_client.dart:225` vs `:229` |
| 10 | 通道凭据的**使用情况今天就能从审计日志汇总出来**（原型已跑通）：`boxops` 39 次、`ro-probe-*` 各 40 次（10 分钟巡检）、已撤销的 `gate-*` 仍有历史计数 | 见 1.4；日志在 `/www/wwwlogs/box-ops-access.log`（239 行 / 26 KB，随面板日切） |
| 11 | 删服务器这条路径**是干净的**（口令与令牌都清了 Keystore）：`clearPassword` + `clearApiToken` 都在删除分支里 | `server_ops_settings.dart:497-500` |
| 12 | 文件页的搜索只过滤当前目录（无跨目录搜索）；服务端也没有 find 类动作 | `files_tab.dart:1122`（291 记的是当时的行号）；`box_ops_api.py:555` 的动作表 |

### 1.1 与既有文档不符之处（先说这个）

1. **291 说 A11 是"30 秒"，今天复测是 54 秒** —— 同一台机器、同一目录口径（758 条目），代价翻倍。
   不改变结论（这一项要做），但**改变优先级**：它是目前唯一还能让 App 表现为"卡死"的地方。
2. **291 的 A12 只写了"上传若探测不支持续传，就只做重试"**，没写这条硬约束：**绝不能发
   `Content-Range`** —— rclone 会忽略它并把文件覆盖成"这一段"，是**静默的数据损坏**（实测见 1.3）。
   凡是"分段上传"的想法都到此为止。
3. **"悬挂 API 0 个"不等于"每个方法都有界面入口"**（事实基线第 7、8 条）：`logs` 全程合格地
   挂在闸门外。这是本方案新增 F1 的原因。

### 1.2 大目录的代价（复现）

```bash
PW=$(cat /root/.secrets/box-ops-webdav.password)
for d in usr/lib64 etc root; do
  curl -s -o /tmp/pf.xml -u "boxops:$PW" -X PROPFIND -H 'Depth: 1' \
    "https://box.hpa888.top/dav/$d/" -w "$d %{size_download} B %{time_total}s\n"
done
# usr/lib64 484517 B 53.97s（758 条） / etc 129544 B 1.83s（231 条） / root 148565 B 2.70s（248 条）
```

根因不变：`--dir-cache-time 0` 是刚需（否则新上传的文件看不见），所以**服务端这条路降不下来**，
只能在客户端把体验做对。

### 1.3 续传的边界（今天探明，决定 A12 的形状）

```bash
# 上传侧：先 10 字节，再用 Content-Range 发后 5 字节
curl -u "boxops:$PW" -T <10B>  .../tmp/ops-resume-probe.bin        # 201，大小 10 B
curl -u "boxops:$PW" -H 'Content-Range: bytes 5-9/10' -T <5B> ...  # 201，大小变成 5 B ✗
# 下载侧：
curl -H 'Range: bytes=5-'  → 206，5 B，内容 56789
curl -H 'Range: bytes=0-4' → 206，5 B，内容 01234
curl -H 'Range: bytes=-3'  → 206，3 B，内容 789
```

结论：**下载可以续传，上传不能**；上传侧还要主动禁止这个头（否则客户端一"优化"就损坏文件）。

### 1.4 通道凭据使用情况（原型）

```bash
ssh hpa888 'awk "{u=\$3; if (u!=\"-\") {c[u]++; last[u]=\$4\" \"\$5}} END {for (k in c) printf \"%-28s %5d 次 %s\n\", k, c[k], last[k]}" /www/wwwlogs/box-ops-access.log | sort -k2 -rn'
# ro-probe-hpa888 40 次 / ro-probe-175 40 次 / boxops 39 次 / gate-rw-<已撤销> 31 次 …
```

这条汇总现在**只有我会跑**：用户无法在 App 里看到"我的设备用的还是旧口令"。而"旧口令能不能退休"
正是 C1 的最后一步 —— 所以要把它做成**看得见**的（本方案 D3）。

## 二、路线分组（顺序即执行顺序）

### D 组：低风险、能立刻感知（建议本轮）

**D1（承接 A11）. 大目录：条目上限 + 继续加载 + 可取消**

- 现状：`PROPFIND Depth:1` 一次拿全量；`/usr/lib64` 758 条目要 **53.97 秒 / 484 KB**，这 53 秒里
  页面没有进度、没有取消、也没有"这目录很大"的提示。代码：`files_tab.dart:85 _load()`、
  `:96 _entries = entries`（一次 setState 灌全量）、`:1495 ListView.builder`、`:805` 只有一句
  `CircularProgressIndicator`。
- 机制：代价在服务端（`--dir-cache-time 0` 每次都要重新 stat 每一项），客户端唯一能做的是
  "别一次渲染完、别无声等待、允许退出"。
- 改动：① 默认渲染前 300 项，底部给「还有 M 项，继续加载」（M 来自真实条数）；② 请求中显示
  "正在列举（大目录可能较慢）"+ 取消按钮（取消后不再 setState）；③ 区分"服务端 5xx""超时"
  "401/403"三类文案；④ 列目录的 5 秒阈值：超过就提示"该目录较大"。
- 收益：`/usr/lib64`、`/usr/lib/python3`、`node_modules` 这类目录不再表现为"App 卡死"。
- 成本：0.5–1 天。验收：假服务返回 2000 项 → 只渲染 300、点继续 600、取消后无 setState；
  live 用例打真 `/usr/lib64` 断言"首屏可见且未阻塞"。

**D2（新）. 把 `logs` 接上界面：服务端日志尾部（「系统」页新增「日志」小节）**

- 现状：服务端 `act_logs`（`box_ops_api.py:466`）已能读白名单内的日志尾部（实测
  `/www/wwwlogs/access.log` 2 行 ✅），客户端方法在（`server_ops_api_client.dart:229`），
  但 **`lib/` 内零引用** —— 用户在 App 里看不到日志文件（服务抽屉里的那 30 行是 journal，
  另一个接口）。运维最常用的动作之一在 App 里是**缺的**。
- 改动：① 服务端加一个只读动作 `logfiles`（列 `LOG_ROOTS` 下的候选日志文件：名字+大小+时间，
  不返回内容）；②「系统」页新增「日志」小节：选文件 → 尾部 N 行（复用 `logs`，默认 100、上限
  `MAX_LINES=500`）；③ 顺手把 `/tmp` 从 `LOG_ROOTS` 默认值里去掉（`box_ops_api.py:65`，那是自检
  用的路径，生产不该当日志根；自检自己会覆盖它）。
- 收益：`nginx 报错 / 更新服务日志 / 我方的 box-ops 审计` 这些手机上就能看，不必开终端。
- 成本：0.5 天。验收：假服务 + live 各一条；白名单外路径必须 403（已有行为，回归守住）；
  `logfiles` 不返回文件内容（只有元数据）。
- 边界：这是**只读**动作，与写档无关；不发 Content-Range、不下载整文件。

**D3（新）. 通道凭据可见化：「这台设备用的是旧口令还是设备凭据」**

- 现状：C1 落地的是一台设备一条凭据，但**没有任何界面/接口**能回答"旧口令还在被谁用"（1.4 的
  汇总只有手动 ssh 才看得到）。于是下一步"删掉 `boxops` 那一行让旧口令退休"只能靠猜。
- 机制：那四个入口的 nginx access log 里本来就有 `$remote_user`（第十六节专门为此加的）。
- 改动：① 服务端加只读动作 `channel`：解析 `/www/wwwlogs/box-ops-access.log` 的**最近 N 行**
  （默认 5000，可传 `days`），按 `$remote_user` 汇总 —— 次数、最近时间、用过的入口数、状态码分布，
  并标注该用户名**现在是否还在 htpasswd 里**（`legacy` / 在用 / 已撤销）；② App 在**体检面板**加一行
  "通道凭据（最近 7 天）"，旧口令那一行用醒目文案："还在用旧通道口令（39 次）——换完设备凭据后
  就可以在服务端删掉它"。
- 收益：把 C1 的收尾从"说不清"变成"看得见"；也顺手把"已撤销的凭据是否真的不再出现"变成可核对的数据。
- 成本：0.5–1 天。验收：假服务注入一段真实格式的日志 → 汇总数字与用户名正确、不含 URI；
  live 用例断言汇总里出现巡检凭据 `ro-probe-*` 与 `boxops`。
- 边界：只返回**用户名/入口/计数/状态码**，**绝不返回 URI**（URI 里有目录名），只读、有审计。

### E 组：要动传输主路径（单独立项）

**E1（承接 A12）. 大文件：下载续传 + 上传"明确重试"**

- 现状：无 `Range`/`resume`（事实基线第 3 条）；传输中断一律从头。
- 机制（今天探明，见 1.3）：**下载侧** rclone 的 Range 完全正确（三式全 206）；**上传侧不支持
  续传**，而且带 `Content-Range` 的 PUT 会把文件覆盖成"这一段" —— 是静默损坏，必须禁止。
- 改动：① 下载：写 `.part` 分片，同名断点用 `Range: bytes=<已收>-` 续，收完校验大小（可选
  sha256）再改名；② 上传：**不发送 `Content-Range`**，失败重试前先 `DELETE` 目标（避免
  "201 覆盖"语义下留半个文件），文案写明"会从头重传"；③ 传输页显示"已传 x MB / y MB"。
- 收益：几十 MB 的日志/备份包不再因为一次掉线全丢；同时把"静默损坏"这条风险从代码层面关掉。
- 成本：1 天。验收：live 用例 —— 下载 10 MB 文件在 5 MB 处断开后续传，拼接后 sha256 与直连一致；
  上传用例断言请求里**没有** `Content-Range`。

### F 组：工程守卫（顺手做）

**F1（新）. 闸门口径升级：把"只有测试在调"的公开方法单独报出来**

- 现状：`tool/scan_dangling_apis.py:48` 的 `SEARCH = ["lib", "test"]`，于是"接口存在、测试齐全、
  界面零引用"的半成品不会被报（`logs` 就是人肉才发现的，事实基线第 7、8 条）。
- 改动：在现有"零调用即红"之外加**警告档**（不影响退出码）：一个公开方法若在 **`lib/` 内零引用**
  （按**裸名字**统计，把 `client.overview` 这种方法引用也算上，避免误报），就列出来，逐条判
  "接线或删掉"。同时把这条口径写进脚本 docstring 的"已知边界"。
- 收益：下次再出现"有接口没界面"的功能，闸门当场说话，不用靠读代码撞见。
- 成本：0.25 天。验收：跑一次能列出 `logs`（本轮接线后应消失）与 `debugAwaitPersistence`
  （判断是接线还是删掉），且 CI 退出码不变。

### 承接未做（不在本轮，单独立项）

**B2. 主机历史服务端化**（289/291 原文照搬，仍未做）：折线历史现在是客户端环形缓冲，换设备即丢。
175 的采集器落 JSONL + 边缘机聚合端点，客户端优先读服务端。成本 1–2 天。

## 三、明确不做

| 不做 | 理由 |
|---|---|
| 接宝塔面板 API | 权限等同 root，key 进 APK 即失守；289 已否决，不重开 |
| 上传侧分段/断点续传 | **实测会静默截断文件**（1.3）：服务端这么实现的，做了就是把"掉线重传"换成"文件损坏" |
| 把文件通道收成"白名单目录" | 会破坏你要的"整个盘"；白名单形态只用于 C2 的动作与路径保护名单 |
| 权限/属主**列**（列表里显示） | WebDAV 的 PROPFIND 不返回该属性；要显示得靠 C2 额外接口，性价比低（改权限的入口已有） |
| 文件列表缩略图 / 客户端打包上传 / 非 root 终端 / PHP·数据库·防火墙管理 | 291 已逐条写明理由，不变 |
| 跨目录搜索（find） | 服务端现在没有 find 动作，加一个"按名字递归找"要新写白名单动作 + 超时/结果上限；本轮 D2/D3 已经要扩两个只读动作，**先不叠**（列在这里，想加就单独一提） |
| 容器/定时任务小节（`docker ps`、`crontab -l`） | 现在这两台机器上只有 Kuma 一个容器、cron 里都是我们自己装的；收益不如日志小节，留档 |

## 四、验证计划

- 每项：单测先行（假服务注入、不联网），一个项目一个提交（提交信息带编号）；
  一组做完跑 **CI 口径三条**：`flutter test --exclude-tags live`、`flutter analyze --no-fatal-infos`
  （0 error / 0 warning）、`python3 tool/scan_dangling_apis.py`（悬挂 0；F1 之后多一档警告）。
- live（真服务端，显式给宽松超时）：D1 打真 `/usr/lib64`；D2 的 `logs` 白名单内外各一条；
  D3 的 `channel` 汇总出现 `ro-probe-*` 与 `boxops`；E1 的下载续传 sha256 一致 + "请求里没有
  `Content-Range`"。
- **上一轮的未验证项（真机）本轮不再原样顺延**，按下面两条办：
  ① 能变成数据的已经变（A9 的"最近请求"、本轮 D3 的"凭据使用情况"）；
  ② 剩下三条只能你自验，给出可复现的三步（每步写清"看到什么算对"）：换机器后文件/终端是否跟着切、
  终端按键手感/断线重连、294 的写按钮在小屏上是否够得着。

## 五、规模粗估（非承诺）

| 项 | 粗估 |
|---|---|
| D1 大目录体验 | 0.5–1 天 |
| D2 `logs` 接线（+`logfiles`） | 0.5 天 |
| D3 通道凭据可见化（+`channel`） | 0.5–1 天 |
| F1 闸门口径 | 0.25 天 |
| E1 下载续传 + 上传重试口径 | 1 天 |
| B2 历史服务端化 | 1–2 天 |
| **建议本组合计（D1+D2+D3+F1）** | **约 2–2.75 天** |

## 六、实施记录（1.20.38 / 295 已经做完的）

本轮（拍板结果：**1. 按建议、2. 旧口令直接退休、3. B2 不排**）落地如下，四件事各一个提交：

| 项 | 提交 | 落地内容 |
|---|---|---|
| D1 大目录 | `c08b1b5` | 一次最多渲染 300 条 + 「还有 N 项，继续加载」；过滤/排序/换目录回到初值；超 5 秒换文案；「停止等待」（传输层没给 `list` 取消能力 —— 它是共享给远端存储插件的 `WebdavClient`，不动它，所以做成"结果回来也不用"并把这件事写在按钮下）。用例 5 条 |
| D2 日志接界面 | `f96af4d` | 服务端新增只读动作 `logfiles`（白名单里有哪些日志、大小、最后修改，两层目录、上限 200、最近的在前）；App 侧「系统」页新增「日志」小节（最近 4 个 + 「看全部」弹层 + 尾巴 100/500 行两档）。用例 +2 |
| D3 通道凭据可见化 | `162be64` | 服务端新增只读动作 `channel`（按用户名 × 入口汇总次数/最近时间/最近 IP/入口与方法分布/被拒次数，与 htpasswd 对照标 `stillValid`、`readOnly`，并列出"签了但没用过"）；App 侧「系统」页加「通道凭据（最近 7 天）」卡。自检 +13 项，用例 +2 |
| F1 闸门口径 | `e792be5` | 见下（比方案里写的大一圈：两个方向都要修） |

### 6.1 实施过程中新发现的事实（都改了）

1. **`logs` 的白名单能被符号链接绕过（真漏洞，已修）**：白名单前缀是在 `resolve()` **之前**
   查的，所以白名单目录里放一条 `x -> /etc/shadow` 就能读它。现在 resolve 之后再查一次
   前缀，符号链接一律不算白名单；自检里加了这条拒绝用例（`logs` 与 `logfiles` 各一条）。
2. **闸门还有反方向的毛病：把方法当回调传（tear-off）不算调用点**。做 D3 时
   `pull('通道凭据', client.channel)` 让闸门报了 **1 个悬挂**——而它明明接了线。
   现在引用口径是 `名字(` **与** `.名字` 两种，tear-off 也算引用。
3. **黄灯档落地后立刻报出一个真的**：`clearMemory`（远端存储插件的缩略图缓存释放）
   `lib/` 内零引用、只有 `test/` 在调 —— 和 283 那次的 `clearThumbnailCache()` 同形。
   它在 `remote_storage`，不属本轮范围，**留在黄灯里可见**（要么接内存压力/离开页面时调，要么删）。
   另一个 `debugAwaitPersistence` 是写明的测试钩子，已进 `ALLOW` 白名单。
4. **日志白名单去掉了 `/tmp`**：生产上不该把 `/tmp` 当日志根；自检本来就覆盖临时目录，
   所以行为不变（`LOG_ROOTS` 现在只有 `/var/log`、`/www/wwwlogs`、更新服务日志三处）。
5. **自检的临时目录不再跟随 `TMPDIR`**：`TMPDIR` 可能指向 `/root/.hermes`（被 du 黑名单
   拒绝的密钥类路径），会让 `diskusage` 那项"因为环境"假失败。
6. **通道访问日志只在边缘机**：`channel` 在看不到日志的机器上回 `available:false` + 指路
   （"换到 hpa888 那条看"），而不是假装"没有凭据在用"。App 侧按同口径渲染。

### 6.2 退休旧通道口令的实测依据

`channel` 上线的第一次真实输出（7 天窗口）就回答了这个拍板问题：

| 凭据 | 状态 | 用量 | 最近 | 来源 |
|---|---|---|---|---|
| `boxops`（**旧口令**） | 在 htpasswd 里 | 65 次 | 09:34 | 47.109.97.1（服务端自己）/ 175 / **手机** |
| `phone-hpa888` / `phone-175` | 在 htpasswd 里 | 各 2 次 | 09:32 | 47.109.97.1（待用户填进 App） |
| `ro-probe-*` | 在（巡检用） | 各 52 次 | 09:50 | 175（预期内的 403 都在里面） |
| `box` / `gate-*`（C1 改造时的实验凭据） | **已撤销** | 17+31+… | 08:39–08:46 | 47.109.97.1 |

手机（`120.32.145.146`）在通道日志里 10 次、全部用旧口令 `boxops`，最后一次 09:27:31
翻的是 `/root/qin-flight/`。**结论：必须先让手机换成设备凭据，再删旧口令那一行** ——
顺序反了就是"App 当场 401，而读新凭据的路也一起断了"。

### 6.3 真机反馈追出来的两项（296，用户截图当天就做了）

用户贴完设备凭据后截图体检：文件通道 ✅、终端 ❌ 401。查日志发现两处**与凭据无关**的
原因（详见 `docs/ops_channel_20260925.md` 第十七节）：

| 项 | 提交 | 内容 |
|---|---|---|
| D4 地址静默串机器 | `8dc0df6` | 终端地址的兜底写死成主服务端的 `/term/`（只有 apiUrl 按机器分）→ "175 的凭据 + 主服务端的终端"必然 401。改为三样默认值都按机器 id 取、用户自己加的机器宁可空着、终端地址还能从 `/dav175` 推 `/term175/`、用户名空不发凭据 |
| E2 WebView 凭据缓存 | `818c0c8` | WebView 按 (源 + realm) 缓存 Basic 凭据，缓存命中时 App 的应答回调**不会被调用** → "换了口令，终端还在用旧身份"。原生加一条清缓存通道 + 终端页凭据变了先清 |

服务端配套（不发版）：4 个通道 location 的 `auth_basic` realm 升到 `*-v2`
（换 realm = 换保护空间，**已装的老版本 App 也会重新问一次**），脚本带自动回滚。

### 6.4 界面不再把人往错的地方带（297，用户说"顺便优化一下 UI"）

用户填 175 那台时踩的坑，一半在代码（D4），一半在**弹窗自己的暗示**：示例地址写死
主服务端那台 —— 加了 175 的用户看着 `https://box.hpa888.top/dav175` 这种示例，
把它当成"这里该填主服务端"。所以这一轮把弹窗与列表都改了：

| 改动 | 内容 |
|---|---|
| 示例地址按机器 | 弹窗里三格的示例都按你这台机器算（`dav175` → `term175/` → `opsapi175`），并**跟着你输入的文件地址实时变**；自己加的机器推不出来就空着 |
| 顶上一句"还缺什么" | 进弹窗第一眼看到「这台还缺：口令、只读接口地址」或「四项都齐了」 |
| 终端/只读接口说清来源 | 「留空就用上面文件地址推出来的这个」/「留空 = 不看服务器状态」 |
| 一键补齐 | 填了文件地址后，「按文件地址补齐上面两格」把终端与只读接口一起写好 |
| 串机器护栏 | 文件地址是 `/dav175` 而终端地址还是 `/term/` 时，当场红字说明"会拿这台的凭据去连另一台的终端，多半是 401"，并给「改成对的那台」一键修；保存前再确认一次 |
| 地址校验 | 明显写错的地址（空格、缺主机、只有 `https://`）拦住保存并指出是**哪一格**；只写主机（`box.hpa888.top/dav175`）保存时自动补 `https://` |
| 列表/切换器看得到终端状态 | 每台那一行现在有机器 id、口令、系统页令牌、以及「终端：已配 / 还没配 / ⚠ 看着指向另一台」 |

顺带修掉一个同类小毛病：`terminalUrlFromDav` 只认带协议的 `https://host/davX`，
而真机上用户填的是 `//host/dav175`（协议省了，照样能连）—— 于是"留空自动推"对他
反而推不出来。现在两种写法都认。

## 七、待拍板（3 条，附建议）

1. **本轮做哪些？** 建议 **D1 + D2 + D3 + F1 一版发**：都是低风险、都能立刻感知，
   而且 D3 是"旧口令退休"这件收尾事的前提。E1 单独一版 —— 它要改传输主路径，值得配一次
   真机验证（续传这种东西在真机弱网下才会露问题）。
2. **旧口令（htpasswd 里的 `boxops`）什么时候退休？** 建议：**D3 上线后观察一周**，
   确认汇总里只剩设备凭据（外加巡检的 `ro-probe-*`），再删那一行 + 重启 rclone + reload nginx。
   顺序很重要：**先让设备都换成设备凭据，再退休旧口令**，否则那台设备当场 401。
3. **B2（历史曲线服务端化）排哪一版？** 建议：不排本轮；E1 之后单独一版（1–2 天），
   它只影响"曲线好不好看"，不影响能不能管服务器。

## 6.5 D7：切了机器"没有显示"（2026-09-26）

用户真机反馈："切换服务器一个显示当前，一个切换了没有显示"，截图里顶部切换器写着
**「新服务器」**、服务器页两张卡片**都没有「当前」**标记。

服务端没问题（快照里两台都在），三个问题都在客户端：

| 现象 | 根因 |
| --- | --- |
| 顶部显示「新服务器」 | 新建服务器的默认名就是它，用户没改名 → 切到这台后顶部一直显示它 |
| 两台卡片都没有「当前」 | 「当前」是按 `host.id == currentServer.effectiveSnapshotId` 打的；自建条目的 `snapshotId` 默认等于自己的 id（`srv1`），而快照里的 id 是 `hpa888` / `tencent175` → 对不上 → 永远打不上 |
| "切换了没有显示" | 上面两条的合成：列表里"设为当前"那个圆点**永远画的是空心**（`radio_button_unchecked` 写死），切了也没有任何反馈 |

改法（都在客户端，服务端未动）：

1. **规整数据**（`ServerOpsSettings.normalizeForSave` / `normalized`）：
   * 名字还是占位名（`新服务器`）→ 按地址取名（地址与内置某台相同就用那台的名字，否则用"主机+路径"）；
   * 自建条目的**快照 id 按地址对齐** → 撞内置 175 的条目会挂到 `tencent175`，卡片上就能打上「当前」；
   * 选中的那台"连地址都没有"→ 换成第一台有地址的（别把整页指向空）。
   **读的时候也规整一遍**（`load()` 里 `.normalized()`）—— 已经存进本机的旧数据不能等用户手动保存才恢复。
2. **"设为当前"的圆点反映状态**：当前那台是实心 + 禁用 + tooltip「已是当前机器」。
3. **撞了内置的条目直说**：副标题提示「⚠ 和「腾讯云 · 构建/监控机」是同一台机器 —— 用那条更省事，这条可以删掉」（只对"自建撞内置"这个方向提示，不去烦内置那两条）。
4. **服务器页多一行兜底说明**：当前这台在快照里找不到对应 host 时，直接写出来（"当前这台（X）不在监控快照里 —— 快照里是：hpa888、tencent175"），不再让它静默地什么都没有。
5. 名称输入框提示改成「留空就按地址自动取名」（以前是一句示例名，等于鼓励留空之后顶着"新服务器"跑）。

地址比对（`addressKey`）按"协议无关 + 结尾斜杠无关"归一：真机上填的是 `//box.hpa888.top/dav175` 这种省协议的写法。

## 七、D8：直接在 App 里改文件（2026-09-26）

以前改一行配置要在文件页预览、再装个第三方编辑器、再输一遍口令。这一版把这一步收进 App。

### 7.1 服务端一行没改

需要的能力本来都在：读（`readUpTo`）、写（WebDAV `PUT`）、备份（`COPY`）、改名（`MOVE`）、
以及"密钥类路径进不来"的通道黑名单。所以这版**只动客户端**。

### 7.2 做了什么

| 位置 | 内容 |
| --- | --- |
| 文件页 → 文本预览 | 多一个「编辑」按钮（原来只有「关闭」） |
| 新页面 `file_editor_page.dart` | 全屏等宽编辑器：保存（顶栏，脏了才亮）、查找/替换、历史版本、底部符号键行（`#` `/` `{` `}` `$` …） |
| 新文件 `server_ops_text_edit.dart` | 纯逻辑：二进制/UTF-8 判定、原风格（BOM/结尾换行/CRLF）检测与回写、备份命名与清理、锁门名单 |
| 服务层 | `statFor`（大小/修改时间）、`listBackups`、`saveText`、`backupBeforeSave`、`restoreBackup` |

### 7.3 护栏（这一类功能的坑都在这）

1. **只给文本**：有 NUL 字节 = 二进制；不是合法 UTF-8（GBK 等）= 只读并说明
   —— 读进来乱码、存回去就是把好文件改坏；超过 256KB = 只读（预览另有 64KB 档）
2. **保存前必须备份**：内容 `COPY` 成 `hosts.box-bak-YYYYMMDD-HHMMSS`，同目录、看得见、
   只留最近 3 份（多的自动删）。**备份失败就抛错、不放行保存** —— 没有后路的写入不做
3. **锁门名单多一道确认**：`sshd_config` / `fstab` / `passwd·shadow·sudoers` /
   `authorized_keys` / 通道自己的 `htpasswd` / nginx 配置 / systemd 单元 / 网络配置 / 引导文件。
   **不是禁止**（`/etc/nginx/*.conf` 恰恰最常要手改），而是把"改坏了的后果"写在确认框里；
   顶栏平时就挂一条提醒，不是等到保存才说
4. **改动检测**：打开时记 size+modifiedAt，保存前重取一次；期间被别人改过就问一句
5. **按原风格回写**：BOM、结尾换行、CRLF 都跟着原文件走 —— 否则一次保存会把整个文件的
   换行改掉，diff 一屏红（`TextEditingController` 会把 CRLF 归一成 LF）
6. **恢复也能撤销**：恢复某个历史版本前，先把当前内容也备份一份

### 7.4 怎么验的

* 纯逻辑 11 条：二进制判定、UTF-8 判定、BOM/LF/CRLF/结尾换行的检测与回写、备份命名与
  "只留 3 份"、锁门名单（**含负例：`/etc/hosts` 与 `/root/box/lib/main.dart` 不许打扰**）
* 编辑页 8 条：保存顺序（**备份必须早于上传**）、按原风格回写、二进制/非 UTF-8/超大三种只读、
  锁门确认（点「先不改」就不上传）、期间被改过的询问、全部替换、符号键插入
* 全量 CI：analyze 0 error/0 warning、`flutter test --exclude-tags live` 全绿

## 八、D9：服务器上"打包"与"清理"（2026-09-26）

### 8.1 服务端新增两个写动作

| 动作 | 干什么 | 硬护栏（服务端里写死，客户端改不了） |
|---|---|---|
| `compress` | 把一个文件/目录打成 `.tar.gz`（或 `.zip`），落在同目录或指定目录 | 同名包已存在 → **409 不覆盖**；包落在它自己的输入目录里 → 400（tar 会把刚生成的包也收进去，越打越大）；输入 > 4096MB → 400；目标分区剩余 < 输入 + 256MB → 400；失败时删掉半成品包 |
| `cleanup` | 清三类公认可以清的东西：`journal`（系统日志）/ `tmp`（/tmp 陈旧文件）/ `apt`（软件包缓存） | 配额与保留期**写死在服务端**：journal 只清到剩 200MB、/tmp 只删 7 天没动过的**普通文件**（目录、软链接、最近用过的都不动）；调用方只能选"清哪一类"，选不了"清多狠"；`dry=1` 只报数不动手 |

两件事都走写作用域（令牌要 `--write`）、都进审计、都被 `WRITE_DENY_PREFIXES`（`/root/.secrets`、`/root/.ssh` 等）挡住。

**为什么把配额放服务端**：这两台是生产机，"清到什么程度"不该由手机上的一个参数决定 —— 手机上按错一个数，日志就清光了。`cleanup` 的响应里会回 `limits`（`journalKeepMB`/`tmpKeepDays`），App 直接显示服务端给的数字，**自己不留一份写死的**（改了这边，那边的文案跟着变，不会漂）。

### 8.2 App 侧

- **文件页**：条目菜单新增「打包成 .tar.gz」——目录和单份文件都能打（"把站点目录/一份大日志收成一个包再下载"是运维最常干的事之一）。确认框写明"同名包不会覆盖、大目录要等十几秒"；压完自动刷新列表，长按那个包就能下载。
- **系统页 · 目录占用**：
  - 列表**按占用倒排**（`du -h` 给的是 `1.2G` / `9.0M` 这种字符串，按字符串排会把 9.0M 排到 10G 前面 —— 屏幕上看着正常，没人会发现最大的被埋在下面）；
  - 每行一条占比条（分母取**最大的那一项**，不是子项之和：`du` 的子项之和常比父项还大，拿和当分母会画出一堆不满的条）；
  - **点一行钻进去** + 「上一级」+ 「从根目录开始找」：找"谁把盘吃满了"就是这两步；
  - 「清理」三个按钮：先 `dry` 问一句"能清多少"，把数字摆出来再说要不要真清；清完报"清出 77.2 MB"（没清出东西就说"本来就很干净"，不说"清出 0 B"）；只读令牌下按钮直接禁用并说明原因（不是点了才 403）。

### 8.3 边界（有意不做）

- 不接宝塔面板 API（面板权限等同 root，key 放客户端等于交出服务器）。
- 不做 docker/systemctl 级别的"大扫除"（`docker prune`、`apt autoremove` 这类误删成本太高，真要清就到文件页自己删）。
- 打包不进"云端压缩"：包就落在那台机器上，下不下、删不删由你在文件页决定。

## 九、D10：体检卡 + 服务端历史曲线（2026-09-26）

### 9.1 体检卡（系统页）

一眼看到"整条链路还活着吗"：**证书还剩几天 / 站点在不在线 / 隧道单元 / 巡检最近一次**。

这四样坏起来**全是静默的** —— 页面照常刷新、数字照常显示，直到某天通道全黑。写下这段的当天就踩到两次：把 `box-ops175-tunnel.service` 在 175 上查（单元其实在 hpa888 上）误判成"隧道断了"；读证书先读了宝塔目录下那批**软链已断**的残留，差点误判成证书要到期。

数据**全部来自已有的三处**，没有新采任何东西：
- 站点快照 `monitors.json`：175 每 2 分钟采 Uptime Kuma，里面**本来就有 `certDays`**（`security-patrol.py` 的 `check_certs()` 一直在读它）；
- 只读接口的服务列表：按 `q=tunnel` 查一次，看隧道单元 active 与否；
- 巡检日志 `/var/log/security-patrol.log`：`/var/log` 本来就在日志白名单里。

顺带补的两处：
- `logs` 只读动作的响应加上 `mtime` / `sizeBytes` —— 没有它就说不出"巡检最近一次跑在什么时候"，而这是体检要看的第一件事；
- hpa888 的 acme 续期 cron 原本把输出丢进 `/dev/null`（**续期失败零痕迹**，证书到期那天才知道），改成追加到 `/var/log/acme-renew.log`。

证书阈值：30 天提醒、14 天算急（与巡检脚本同一口径）。

### 9.2 服务端历史曲线（主机页）

`gen_hosts_json.py` 增加环形缓冲：每台机器 720 点（每 2 分钟一点 = **24 小时**），存 `cpu/mem/disk/load` 四条，随 `hosts.json` 一起推给边缘机 —— 塞进同一个文件里，边缘机 nginx 一行都不用改。

- 值为 **null 表示没采到**（机器离线），画线时丢掉而不是塞 0：塞 0 会把"没采到"画成"CPU 0%，很闲"，跟事实相反；
- 时间戳与各条曲线**先对齐再追加**：分别裁剪的话某次异常会让它们各自漂开一格，之后画出来就是"曲线比时间轴少一个点"；
- App 侧默认看 24 小时，可切近 1 小时 / 6 小时；文案标明这段线是"服务端历史"还是"本机记录"（本机那份只活在"你打开过 App 的那段时间"里，留着当兜底）。
