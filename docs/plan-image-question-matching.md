# 只补「无图判断不了」的题 —— 执行方案 v4（最终）

> **v4 相对 v3 的关键修正（实测发现，必须说清）**
> 你的样图 `img_125182c9d7a9.jpg`（SHA-256 `5845e151…`）**已经在题库里了** ——
> 它挂在 `q_Wpj8spyZB4Lb`「这个标志是何含义？」→ 十字交叉路口，且 `imagePerceptualHash` /
> `imageRegionHash` **都已写入**（`936d22b4b5349a20`）。
> ⇒ **整条补图链路（选图→上传→存指纹→下发）已经真实跑通过至少一次**，
> 不是我 v3 推测的"通路断了、从没跑过"。v3 那段说法**我撤回**。

---

## 一、真实链路（已读码 + 实测确认）

```
管理面板选图 → uploadQuizImage(base64) → 服务端存盘 /api/quiz/images/<file>
             → 返回 url → set_image 写 image 字段
```

| 环节 | 真实代码 | 事实 |
|---|---|---|
| 上传 | 服务端 `:2060` `uploadQuizImage` | base64 → 落盘，**服务端只算文件名用的 sha256 前16位**，不算 dHash |
| 绑定 | 服务端 `:2245` `set_image` 分支 | `old.copyWith(image: image)` —— **只写 `image`，不写任何 hash** |
| 下发 | `/api/quiz/sync` 增量 | 图片字段正常下发 ✅ |
| 指纹 | `fromRequest` 读请求体的 `imagePerceptualHash`/`imageRegionHash` | ⇒ **指纹由客户端算好传来** |
| 消费 | 引擎 `_bestImageScore:748` | 用 `imageRegionHash` 算汉明相似度 |

**⇒ 结论：链路通。但 App 的 4 个补图入口全都只传 `image`、不传指纹**
（`quiz_bank_tab.dart:701 / 750 / 948 / 1045`；client `bulkUpdateQuestions` 参数表里根本没有 hash 字段）。
`q_Wpj8spyZB4Lb` 的指纹是**早前另一条（非面板）路径**写进去的。

**⇒ 所以对你最重要的一句话**：
**你传的图会正常显示，但"两个候选二选一"的消歧仍然不会发生 —— 除非把指纹一起补上。**

---

## 二、要传多少张（实测全库 3533 条）

| 分类 | 条数 | 要传图吗 |
|---|---:|---|
| 已有图 | 4 | 已完成 |
| **图题但无图** | **1297** | ↓ 细分 |
| 文字题（无需图） | 2232 | 不用管 |

**1297 条细分（"只补判断不了的题"的精确边界）：**

| 细分 | 条数 | 处置 | 传图 |
|---|---:|---|---|
| **同题干、答案互相冲突** | **524** | 每条各配一张 | ✅ **要** |
| 题干唯一、答案依赖图 | 715 | 建议补 | ⚠️ 建议 |
| 同题干、答案一致 | 58 | 归档重复即可 | ❌ **不用** |

### ⇒ 硬核 = **524 条**；加上 715 = **1239 张**

**TOP 冲突组**：

| 题干 | 条数 | 不同答案数 |
|---|---:|---:|
| 这个标志是何含义？ | 238 | 217 |
| 图中交通标志是指（ ） | 22 | 16 |
| 这个标志是什么含义？ | 17 | 16 |
| 机动车仪表板上如图所示指示灯亮表示什么？ | 14 | 14 |

**⚠️ 这里 238 条不是重复，是 238 道不同的题** —— 题干碰巧一样，答案由各自那张图决定。
**没图判不出对错，归档哪条都可能误删真题 ⇒ 只能靠补图解决，不能靠清理。**

---

## 三、图片规格实测结论（决定你怎么传）

用你的样图（578×578 纯标志图）跑真实 dHash 稳定性测试：

| 处理 | 汉明距离 | 结论 |
|---|---:|---|
| 缩放 50% / 200% | **0** | ✅ 无所谓 |
| 叠加水印文字 | **0** | ✅ **无所谓**（带水印的图可直接用） |
| JPEG 压到质量30 | 1 | ✅ 无所谓 |
| 加白边 20px | **6** | ⚠️ **会破坏** |
| 裁剪 5% 边 | **13** | ⚠️ **会破坏** |

### ⇒ 三条硬规矩
1. **别裁、别加白边、别留边框** ← 唯二会破坏匹配的操作
2. **水印/尺寸/格式/压缩全无所谓**（你的样图 18KB JPEG 完全可用）
3. 保持原始构图

---

## 四、执行：三步，只有第 2 步要你动手

### 第 1 步（我做）：把指纹补上 —— 唯一阻塞点

改 4 个补图入口，上传时复用**已有**的 `QuizQuestionImageStore.computeDHash(bytes)`
（`quiz_question_image_store.dart:62`，9×8 灰度 dHash，输出正好是引擎校验的 `^[0-9a-f]{16}$`），
把 `imagePerceptualHash` + `imageRegionHash` 随请求一起送。

- 服务端 `fromRequest` 已能接收 → **服务端零改动**
- `bulkUpdateQuestions` / `completeIncomplete` / `update` 的 body 里加两个字段
- **⚠️ 需构建新版 APK 你才能用**

### 第 2 步（你做）：照工单传图
- 我先出**补图工单**：按题干聚类，标注每组已有答案、缺几张
- 顺序：冲突的 524 条 → 单条 715 条
- 随传随生效（走增量同步），**不用再发版**

### 第 3 步（我做）：验证 + 收尾
- 真实引擎复测：`n=2 ambiguous` → `hit` 单答案
- 58 条真重复归档
- 出验收报告

---

## 五、待你拍板

1. **第 1 步现在就改代码吗？** ← 唯一阻塞点（改完要构建新版 APK）
2. **第一批范围**：只做 524 条冲突的？还是 524+715 全做？
3. **你的图能批量拿到吗**（题库导出 / 网盘），还是逐张找？—— 决定工单怎么排

---

## 六、诚实声明

**已验证（读码 + 实测）**
- 全库 3533 条统计、1297 图题无图、524 冲突、58 真重复 → 真实增量接口拉取
- 你的样图**已在库**（`q_Wpj8spyZB4Lb`，SHA-256 `5845e151…` 字节级一致，从服务端下载比对确认）
- 该题 `imageRegionHash=936d22b4b5349a20` 非空，`revision=4`
- 图片 dHash 稳定性（缩放0/水印0/裁剪13）→ Python 复刻 Dart 算法对真实样本实测
- 补图四入口只传 image 不传 hash → 真实读码 `quiz_bank_tab.dart:701/750/948/1045` + client 签名
- 服务端 set_image 只 `copyWith(image:)` → 真实读码 `:2245-2260`
- 服务端 uploadQuizImage 只算 sha256 前16位当文件名 → 真实读码 `:2096`
- 引擎消费 `imageRegionHash`、校验 `^[0-9a-f]{16}$` → 真实读码 `:748-756`

**已查清（原「未验证」项已收口）**
- 我曾发现「我用 Dart 算法算样图得 `14323269f096b2c8`，库里存 `936d22b4b5349a20`，不一致」。
  **现已定位原因，不是缺陷**：
  - 服务端**完全没有 dHash 逻辑**（真实 grep 确认，只有 passwordHash）⇒ 指纹必来自客户端
  - 客户端有两套同算法实现：Dart `computeDHash`（`quiz_question_image_store.dart:65`）与
    Kotlin `computeDHash`（`QuizAccessibilityService.kt:3437`），均为 **9×8 灰度、行内左>右、16位hex**
  - Kotlin 版用 `Bitmap.createScaledBitmap(..., true)` = **双线性**；我的 Python 复刻用 PIL 默认重采样，
    9×8 这种极小尺寸下**重采样差异会翻转 bit**（实测 6 种滤波器无一命中）
  - ⇒ `936d22b4b5349a20` 是**客户端原生算的**，与**同为客户端原生算的探针 hash** 同源可比
  - **关键**：引擎比的是「探针 hash ↔ 库内 regionHash」，**两者都出自同一套原生实现** ⇒ 生产环境一致性成立
  - ⚠️ 但**仍有一项待实测**：拿到真机后，用面板上传同一张图，比对「库里存的 hash」与「真机算的 hash」是否逐位相同
- 引擎消费 `imageRegionHash`、校验 `^[0-9a-f]{16}$` → 真实读码 `:748-756`
- 补图四入口只传 image 不传 hash → 真实读码 `quiz_bank_tab.dart:701/750/948/1045` + client 签名
- 服务端 set_image 只 `copyWith(image:)` → 真实读码 `:2245-2260`
- 服务端 uploadQuizImage 只算 sha256 前16位当文件名 → 真实读码 `:2096`
- 服务端无任何 dHash → 真实 grep `:849-6923` 仅见 passwordHash
- 阈值 **75分 / 领先8分**（`quiz_engine.dart:434/439`）是既有常量，我**未改动也未实测**；
  **调大更保守（更多人工确认），调小更激进（可能误自动作答）**
- 1297 条按关键词圈定，个别题文字上其实可答，**出工单时逐条回读复核**

**遗留中间态**
- `q_TeFxtRzO2sqf` 题干已改但未配套归档，需与 58 条真重复一并收口
